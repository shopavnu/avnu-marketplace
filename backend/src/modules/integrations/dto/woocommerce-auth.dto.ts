// @ts-strict-mode: enabled

/**
 * DTO for WooCommerce authentication
 * This is a placeholder file to satisfy the import requirements
 * Since we're adopting a Shopify-first approach, this structure is minimal
 */
export class WooCommerceAuthDto {
  storeUrl: string;
  consumerKey: string;
  consumerSecret: string;
  version?: string;
}
