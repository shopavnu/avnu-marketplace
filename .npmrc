# Force npm to accept inconsistencies and override strict versioning
save-exact=true
legacy-peer-deps=true
strict-peer-dependencies=false
engine-strict=false

# Allow for package-lock inconsistencies (use with caution)
package-lock=false

# Override multer version everywhere
multer@="2.0.1"

# Required for some dependencies
better-sqlite3_binary=true
